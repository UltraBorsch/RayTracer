#include "Structs.h"

Intersection Intersection() {
    Intersection intersection;
    return intersection;
}

Ray Ray(float3 o, float3 d) {
    Ray ray;
    ray.origin = o;
    ray.direction = d;
    return ray;
}
